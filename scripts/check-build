#!/bin/bash

echo "pre-push: Running a build to ensure documentation and bundle is up-to-date"

npm run build

if git ls-files -m lib.js API.md | grep .
then
    echo "pre-push: Error - uncommited changes to lib.js; please commit those before pushing"
    exit 1
fi
